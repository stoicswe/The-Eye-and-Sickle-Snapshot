# A TEST-ONLY pseudo-language. Never shipped: it lives in src/test/resources, and `zz` is not in
# the Language enum, so no player can select it. It exists so the overlay chain is proved against a
# real properties file rather than asserted about an empty set — the failure this catches is the
# whole layer being present, wired, and reaching nothing.
ui.calc.base = ZZBASIS
settings.cat.desk = ZZTISCH
